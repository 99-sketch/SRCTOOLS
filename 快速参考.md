# SRC 执行器 - 快速参考

## 文件夹位置
`D:\SRC执行器`

## 目录结构

```
D:\SRC执行器\
├── dist\
│   └── 教育SRC挖洞工具.exe    # 主程序（双击运行）
├── src\                        # 源代码
│   ├── recon\                  # 信息收集模块
│   ├── vuln\                   # 漏洞挖掘模块
│   ├── exploit\                # 主动验证模块
│   ├── webshell\               # Webshell管理模块
│   └── report\                 # 报告生成模块
├── data\                       # 数据文件（高校列表等）
├── outputs\                    # 输出目录（报告/Webshell等）
├── gui_app.py                  # GUI主程序
├── run.py                      # 命令行入口
├── edu_src_gui.spec            # PyInstaller打包配置
└── README.md                   # 详细说明文档
```

## 快速开始

### 方式一：直接运行 exe
```
双击 dist\教育SRC挖洞工具.exe
```

### 方式二：从源码运行
```powershell
cd D:\SRC执行器
py -3 gui_app.py
```

### 方式三：命令行模式
```powershell
cd D:\SRC执行器
py -3 run.py --school 清华大学
```

## 重新打包

修改源码后重新打包：
```powershell
cd D:\SRC执行器
py -3 -m PyInstaller --noconfirm edu_src_gui.spec
```

## 功能模块

| 模块 | 功能 |
|------|------|
| 资产收集 | 子域名/端口/指纹/目录 |
| 漏洞挖掘 | nuclei/afrog/xray/53个工具集成 |
| 主动验证 | SQL注入/XSS/路径穿越/反序列化 |
| Webshell管理 | 哥斯拉/冰蝎/蚁剑/天蝎/C2框架 |
| 命令执行 | 复制粘贴/常用命令/历史记录 |
| 报告管理 | 增删改查/缓存/导出 |

## 集成的工具（53个）

### 核心扫描器
- nuclei, afrog, xray, ehole, P1finger, fscan, Rscan, httpx, sqlmap, oneforall

### 信息收集
- TscanPlus, xscan, kscan, Railgun, massdns, Fine, golin, goon

### 中间件漏洞验证
- shiro_attack, JNDIExploit, ysoserial, WeblogicTool, ThinkphpGUI, struts2, NacosExploit, JenkinsExploit, XXL-JOB, jeecgExploit, RuoYiVueScan

### Web漏洞专项
- SuperSQL, JDumpSpider, WebCrack, DecryptTools, vue_scan, API-T00L

### C2 框架
- CobaltStrike 4.7, dogcs, XieBroC2, Counter-Strike

### Webshell 管理
- 哥斯拉, 冰蝎, 蚁剑, 天蝎, Ether Ghost

## GitHub 仓库
https://github.com/99-sketch/SRCTOOLS

## 二开指南

1. 修改 `D:\SRC执行器\src\` 下的模块
2. 修改 `D:\SRC执行器\gui_app.py` 调整 GUI
3. 运行 `py -3 gui_app.py` 测试
4. 打包：`py -3 -m PyInstaller --noconfirm edu_src_gui.spec`
5. 提交：`git add . && git commit -m "更新内容" && git push`

## 注意事项

- 所有输出文件存放在 `outputs\` 目录
- Webshell 连接信息保存在 `outputs\webshell_connections.json`
- 报告缓存保存在 `outputs\reports_cache\`
- 工具路径配置在 `src\recon\tooling.py`
