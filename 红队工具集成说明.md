# 红队工具集成说明

## 集成概览

本次更新集成了 F:\One-fox\tools 中 35 个红队常用工具，覆盖 7 大类别：

| 类别 | 工具数 | 代表工具 |
|------|--------|----------|
| PoC/漏洞利用 | 5 | Gr33k, poc2jar, 0x7eTeamTools, hvvoa, yongheng |
| 信息收集增强 | 4 | enscan, Railgun, sharpscan, cf |
| 漏洞扫描 | 4 | Aazhen, pppscan, dddd, ez |
| 中间件利用 | 7 | Nacos, Jenkins, XXL-JOB, jeecg, RuoYi, TPScan, Spring JNDI |
| 后渗透 | 8 | mimikatz, Ladon, frp, suo5, pingtunnel, goexec, quasar |
| 爆破 | 1 | weekpasswd/JUBILANT-WOLF |
| 辅助工具 | 6 | DecryptTools, mitan, MDUT, RequestTemplate, Serein, poc2jar |

## 自动化集成

### 全流程自动执行

红队工具已集成到「一键全流程」的漏洞挖掘阶段（步骤 5.5），自动执行顺序：

```
1. 资产收集 (run_recon)
2. 漏洞挖掘 (run_vuln)
   ├── 5. 外部扫描器（nuclei/afrog/xray）
   ── 5.5 红队工具（Gr33k/enscan/Railgun/Aazhen/weekpasswd）← 新增
3. 复测 (run_reverify)
4. 生成报告 (run_report)
5. Webshell 生成 (run_webshell)
6. C2 启动（人工操作）
```

### 各工具功能说明

#### Gr33k - CVE 漏洞利用
- **位置**: `gui_scan\Gr33k`
- **功能**: 包含多个 CVE 利用脚本（CVE20192725, CVE20192729 等）
- **自动化**: 自动检测目标是否受影响，命中即记录为漏洞
- **输出**: `external-gr33k`

#### enscan - 企业信息收集
- **位置**: `gui_other\enscan\enscan-v1.2.2-windows-amd64.exe`
- **功能**: ICP 备案、子公司、关联企业信息收集
- **自动化**: 根据学校名称自动搜索企业信息
- **输出**: `external-enscan`

#### Railgun - 自动化信息收集
- **位置**: `gui_other\gorailgun\Railgun.exe`
- **功能**: 综合自动化信息收集（子域名/端口/服务/指纹）
- **自动化**: 自动执行并解析结果
- **输出**: `external-railgun`

#### Aazhen - 综合漏洞扫描
- **位置**: `gui_scan\Aazhen-v3.1-main\Aazhen_Scanner_V3.1.exe`
- **功能**: 批量漏洞扫描，支持多种漏洞类型
- **自动化**: 自动读取目标列表并扫描
- **输出**: `external-aazhen`

#### weekpasswd - 弱口令扫描
- **位置**: `gui_scan\weekpasswd\JUBILANT-WOLF-V2.0.1.exe`
- **功能**: 常见服务弱口令爆破
- **自动化**: 自动检测弱口令
- **输出**: `external-weekpasswd`

## 报告集成

红队工具结果已集成到渗透测试报告中：

### 执行摘要
```
| 红队工具 | Gr33k X / enscan X / Railgun X / Aazhen X / weekpasswd X |
```

### 漏洞来源标注
每个漏洞的 `source` 字段会标注来源：
- `external-gr33k` - Gr33k CVE 利用
- `external-enscan` - enscan 企业信息
- `external-railgun` - Railgun 自动化收集
- `external-aazhen` - Aazhen 综合扫描
- `external-weekpasswd` - weekpasswd 弱口令

## 工具路径配置

所有工具路径配置在 `src/recon/redteam_tools.py`：

```python
TOOLS_ROOT = Path(r"F:\One-fox\tools")

POC_TOOLS = {
    "gr33k": TOOLS_ROOT / "gui_scan" / "Gr33k",
    "poc2jar": TOOLS_ROOT / "gui_scan" / "poc2jar-WINDOWS" / "poc2jar.jar",
    ...
}

RECON_TOOLS = {
    "enscan": TOOLS_ROOT / "gui_other" / "enscan" / "enscan-v1.2.2-windows-amd64.exe",
    ...
}
```

## 扩展指南

### 添加新工具

1. 在 `src/recon/redteam_tools.py` 中添加工具路径：
```python
NEW_TOOLS = {
    "new_tool": TOOLS_ROOT / "path" / "to" / "tool.exe",
}
```

2. 创建运行函数：
```python
def run_new_tool(targets, school_code, stop_flag=None, emit=None):
    _out = emit or print
    exe = NEW_TOOLS.get("new_tool")
    if not exe or not exe.exists():
        _out("    [跳过] new_tool 未找到")
        return []
    
    # 执行逻辑...
    findings = []
    return findings
```

3. 在 `run_all_redteam_tools` 中注册：
```python
tools_to_run = [
    ...
    ("new_tool", lambda: run_new_tool(targets, school_code, stop_flag, emit)),
]
```

4. 在报告生成器 `_src_cn` 中添加来源映射：
```python
"external-new_tool": "new_tool 扫描(自动)",
```

## 注意事项

1. **工具可用性**: 所有工具路径基于 `F:\One-fox\tools`，如路径变更需修改配置
2. **超时控制**: 每个工具默认超时 120-300 秒，防止卡死
3. **错误处理**: 任一工具失败不影响其他工具执行
4. **结果去重**: 自动去重，避免重复漏洞
5. **停止标志**: 支持随时停止（GUI 停止按钮）

## 后续计划

- [ ] 集成更多 PoC 库（如 nuclei-templates 自定义模板）
- [ ] 增加漏洞利用成功率统计
- [ ] 支持自定义工具插件
- [ ] 增加工具执行优先级配置
